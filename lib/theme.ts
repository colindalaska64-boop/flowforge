export function useThemeColors() {
  return {
    text:    "var(--c-text)",
    text2:   "var(--c-text2)",
    muted:   "var(--c-muted)",
    bg:      "var(--c-bg)",
    panel:   "var(--c-panel)",
    card:    "var(--c-card)",
    border:  "var(--c-border)",
    input:   "var(--c-input)",
    hover:   "var(--c-hover)",
  };
}
